from(bucket: "drone_telemetry")
  |> range(start: -1h)
  |> filter(fn: (r) => r._measurement == "GLOBAL_POSITION_INT")
  |> pivot(rowKey:["_time"], columnKey: ["_field"], valueColumn: "_value")
  
  

  important point to configure dashboard in grafana
  add connection as infinite data source and then import json configurtion, it will work